This is a list of calls you can make to change your body.
Most of these are illegal if you use them on mortals so
try to avoid doing that. Enjoy.

call name;set_stats;"stat";#

call name;set_max_hp;#
call name;set_max_sp;#
call name;set_max_mp;#
call name;set(add)_hp;#
call name;set(add)_sp;#
call name;set(add)_mp;#

call name;add_exp;#
call name;add_dev;#
call name;add_alignment;#
call name;set_level;#
call name;set_gender;"gender"
call name;set_race;"race"
call name;set_class;"class"
call name;set_guild;"guild"
call name;set_married;"spouse"
call name;set_max_internal_encumbrance;#

call name;set(add)_skill;"skill";#
call name;delete_skill;"skill"
call name;set_spell_level;"spell";#(1-6)
call name;delete_spell;"spell"

call name;add_intox;#
call name;add_stoned;#

call name;add_money;"currency";#

call name;revive

call name;set_primary_start;"room path"
