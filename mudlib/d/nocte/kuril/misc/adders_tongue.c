#include <std.h>
inherit OBJECT;

void create() {
	::create();
	set_name("clove of adder's tongue");
	set_id( ({"clove", "adder's tongue", "clove of adder's tongue"}) );
	set_short("clove of adder's tongue");
	set_long(
		"A clove of adder's tongue."
	);
	set_weight(1);
	set_value(20);
}
