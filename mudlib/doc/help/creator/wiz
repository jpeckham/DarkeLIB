COMPILING: 	 cc, clone, load, make, renew, tsh, update

FILE HANDLING: 	 cat, cd, clog, cp, diff, du, ed, error, format, from,
	       	 grep, head, log, ls, mkdir, more, mstatus, mv, popd,
	      	 pupdate, pushd, pwd, rehash, replog, reset, rm,
	      	 rmdir, scan, tail, unbundle, work

LIVINGS: 	 banish, force, I, localcmd, locate, mraces, sc, sponsor,
		 wizheal

OBJECT HANDLING: ac, acheck, call, clean, dest, goto, rcheck, stat,
		 trans, wc, wcheck

MISC: 		 classblock, codeblock, domains, earmuffs, echo, echoall, 
		 echoto, eval, gauge, home, invis, lines, langs, malloc,
		 netstat, review, setenv, snoop, newwiz, addnews
		 tellblock, unset, vis, wizlist

OTHER TOPICS: 	 filepath, man, memory, mtp, objectpath, ref, refs, tref,
                 unref

CHANNELS:        cre, admin
