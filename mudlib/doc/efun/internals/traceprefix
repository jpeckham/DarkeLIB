
traceprefix(3)        LPC Library Functions        traceprefix(3)

NNAAMMEE
       traceprefix()  - sets the prefix determining which objects
       to trace

SSYYNNOOPPSSIISS
       string traceprefix( string prefix );

DDEESSCCRRIIPPTTIIOONN
       If the the traceprefix is set (i.e. not  0)  tracing  will
       only occur in objects having a name with the set prefix.

SSEEEE AALLSSOO
       trace(3)

MudOS                       5 Sep 1994                          1

