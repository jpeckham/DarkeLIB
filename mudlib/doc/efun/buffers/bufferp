
bufferp(3)            LPC Library Functions            bufferp(3)

NNAAMMEE
       bufferp() - determine whether or not a given variable is a
       buffer

SSYYNNOOPPSSIISS
       int bufferp( mixed arg );

DDEESSCCRRIIPPTTIIOONN
       Return 1 if `arg' is a buffer value and  zero  (0)  other-
       wise.

SSEEEE AALLSSOO
       mapp(3),  stringp(3),  pointerp(3),  objectp(3),  intp(3),
       bufferp(3),  floatp(3),  functionp(3),   nullp(3),   unde-
       finedp(3), errorp(3)

MudOS                       5 Sep 1994                          1

