
eval_cost(3)          LPC Library Functions          eval_cost(3)

NNAAMMEE
       max_eval_cost() - returns the maximum evaluation cost

SSYYNNOOPPSSIISS
       void max_eval_cost()

DDEESSCCRRIIPPTTIIOONN
       max_eval_cost()  returns  the  number of instructions that
       can be executed before the driver  decides  it  is  in  an
       infinite loop.

SSEEEE AALLSSOO
       catch(3),     error(3),     throw(3),    error_handler(4),
       set_eval_limit(3), reset_eval_cost(3)

MudOS                       5 Sep 1994                          1

