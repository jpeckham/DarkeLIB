
write_prompt(4)           Driver Applies          write_prompt(4)

NNAAMMEE
       write_prompt - called when the parser wants a prompt to be
       written.

SSYYNNOOPPSSIISS
       void write_prompt( void );

DDEESSCCRRIIPPTTIIOONN
       If write_prompt is  present  in  the  player  object,  the
       driver will call it whenever the default prompt would nor-
       mally be printed.  The driver will not  call  write_prompt
       when the player is in input_to or ed.

AAUUTTHHOORR
       Truilkan@TMI

MudOS                       5 Sep 1994                          1

